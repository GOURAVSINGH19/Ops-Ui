---
title: Table
description: A flexible and feature-rich table component for displaying data with sorting, filtering, and pagination.
---

import { Callout } from "@/components/Callout";
import { ComponentPreviewTabs } from "@/components/ComponentPreview";
import {ComponentSource} from "@/components/ComponentSource"
import DempTable from "@/components/usage/table";

## Overview

The Table component is a powerful data display component that provides features like sorting, filtering, pagination, and customizable actions. It's designed to handle both simple and complex data presentation scenarios.

## Installation

```bash
--->    npm install @workspace/ui
```

## Usage

<ComponentPreviewTabs
  component={<DempTable />}
  source={
    <ComponentSource
      src="components/docs/Table.tsx"
      title="table.tsx"
      language="tsx"
    />
  }
/>


## Props

### TableDetails Interface

The Table component accepts the following props:

```typescript
interface TableDetails {
  columns?: ColumnConfig[];
  data?: any[];
  isLoading?: boolean;
  actions?: ActionConfig[];
  sortConfig?: {
    key: string | null;
    direction: string | null;
  };
  onSort?: (key: string) => void;
  emptyState?: string;
  onRowClick?: (row: any) => void;
  className?: string;
}
```

### Column Configuration

Each column in the `columns` array should have:

```typescript
{
  key: string;              // Unique identifier for the column
  label: string;            // Display label for the column header
  sortable?: boolean;       // Enable sorting for this column
  className?: string;       // Custom CSS classes for column header
  cellClassName?: string;   // Custom CSS classes for cells
  align?: 'left' | 'center' | 'right'; // Cell alignment
  render?: (value: any, row: any) => React.ReactNode; // Custom render function
}
```

### Action Configuration

Each action in the `actions` array should have:

```typescript
{
  icon: React.ComponentType;           // Icon component to display
  label: string;                       // Tooltip/label for the action
  color?: string;                      // Button color
  link?: string | ((row: any) => string); // Navigation link
  div?: string | ((row: any) => string);  // Tooltip content
  disabled?: (row: any) => boolean;    // Conditionally disable action
  onClick?: (row: any) => void;        // Click handler
}
```

## Examples

### Basic Table

```jsx
import { Table } from '@workspace/ui';

const columns = [
  { key: "name", label: "Name", sortable: true },
  { key: "email", label: "Email" },
  { key: "status", label: "Status" },
];

const data = [
  { name: "John Doe", email: "john@example.com", status: "Active" },
  { name: "Jane Smith", email: "jane@example.com", status: "Inactive" },
];

export default function BasicTable() {
  return (
    <Table
      columns={columns}
      data={data}
      emptyState="No data available"
    />
  );
}
```

### Table with Sorting

```jsx
import { useState } from 'react';
import { Table } from '@workspace/ui';

export default function SortableTable() {
  const [sortConfig, setSortConfig] = useState({ key: null, direction: null });

  const columns = [
    { key: "name", label: "Name", sortable: true },
    { key: "created_at", label: "Created", sortable: true },
  ];

  const handleSort = (key) => {
    const isAsc = sortConfig.key === key && sortConfig.direction === 'asc';
    const direction = isAsc ? 'desc' : 'asc';
    setSortConfig({ key, direction });
    // Implement your sorting logic here
  };

  return (
    <Table
      columns={columns}
      data={data}
      sortConfig={sortConfig}
      onSort={handleSort}
    />
  );
}
```

### Table with Custom Rendering

```jsx
import { Table } from '@workspace/ui';

export default function CustomTable() {
  const columns = [
    { key: "name", label: "Name" },
    {
      key: "status",
      label: "Status",
      render: (value) => (
        <span className={`px-2 py-1 rounded-full text-sm font-semibold ${
          value === 'active' ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-700'
        }`}>
          {value}
        </span>
      ),
    },
    {
      key: "created_at",
      label: "Created",
      render: (value) => new Date(value).toLocaleDateString(),
    },
  ];

  return <Table columns={columns} data={data} />;
}
```

### Table with Actions

```jsx
import { Edit, Trash2 } from 'lucide-react';
import { Table } from '@workspace/ui';

export default function TableWithActions() {
  const columns = [
    { key: "name", label: "Name" },
    { key: "email", label: "Email" },
  ];

  const actions = [
    {
      icon: Edit,
      label: "Edit",
      onClick: (row) => console.log("Edit:", row),
    },
    {
      icon: Trash2,
      label: "Delete",
      color: "text-red-600",
      onClick: (row) => console.log("Delete:", row),
    },
  ];

  return (
    <Table
      columns={columns}
      data={data}
      actions={actions}
    />
  );
}
```

### Table with Loading State

```jsx
import { Table } from '@workspace/ui';

export default function LoadingTable() {
  const [isLoading, setIsLoading] = useState(true);

  return (
    <Table
      columns={columns}
      data={data}
      isLoading={isLoading}
      emptyState="No data available"
    />
  );
}
```

### Responsive Table

```jsx
import { Table } from '@workspace/ui';

export default function ResponsiveTable() {
  return (
    <Table
      columns={columns}
      data={data}
      className="w-full overflow-x-auto"
    />
  );
}
```

## Features

<Callout type="info">
  The Table component includes built-in support for:
  - **Sorting**: Click column headers to sort ascending/descending
  - **Pagination**: Automatically handles data pagination with configurable rows per page
  - **Filtering**: Search and filter data with custom logic
  - **Custom Rendering**: Use render functions for complex cell content
  - **Row Actions**: Add custom action buttons for each row
  - **Loading State**: Built-in skeleton loading indicator
  - **Empty State**: Customizable message when no data is available
  - **Responsive**: Fully responsive table layout
</Callout>

## Best Practices

1. **Use Unique Keys**: Always provide unique keys for columns to ensure proper rendering and sorting.

2. **Optimize Rendering**: Use the `render` function to customize cell content without modifying the original data.

3. **Handle Large Datasets**: Implement pagination and lazy loading for better performance with large datasets.

4. **Keyboard Accessibility**: Ensure sortable columns are keyboard accessible using the `onSort` callback.

5. **Loading States**: Always show a loading indicator when fetching data to improve UX.

6. **Error Handling**: Use the `emptyState` prop to display helpful messages when data is unavailable.

```jsx
// Good: Using render function for custom content
{
  key: "date",
  label: "Date",
  render: (value) => formatDate(value)
}

// Good: Handling loading states
<Table isLoading={isLoading} data={data} />

// Good: Providing helpful empty state
<Table emptyState="No records found. Try adjusting your filters." data={data} />
```

## Accessibility

The Table component follows WAI-ARIA guidelines:

- Proper semantic HTML with `role="table"`
- Sortable column headers are keyboard accessible
- Row click handlers support keyboard navigation
- Loading and empty states are announced to screen readers

## Customization

You can customize the table appearance using the `className` prop:

```jsx
<Table
  columns={columns}
  data={data}
  className="rounded-lg border-2 border-primary"
/>
```

## Related Components

- [Skeleton](/docs/components/skeleton) - Loading placeholder
- [Button](/docs/components/button) - Action buttons
- [Badge](/docs/components/badge) - Status indicators
